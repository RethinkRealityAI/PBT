/**
 * PLACEHOLDER — replaced by the Test drive implementation (native simulator +
 * trainee-app phone frame). Exists so the Studio shell compiles meanwhile.
 */
import type { TestDriveProps } from '../types';

export function TestDriveStep(_props: TestDriveProps) {
  return <div>Test drive — coming soon.</div>;
}
