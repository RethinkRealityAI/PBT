/**
 * PLACEHOLDER — replaced by the step implementation. Renders nothing useful
 * yet; exists so the Studio shell compiles while steps are built in parallel.
 */
import type { StepProps } from '../types';

export function PetStep(_props: StepProps) {
  return <div>Pet step — coming soon.</div>;
}
