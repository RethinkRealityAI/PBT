/**
 * PLACEHOLDER — replaced by the assistant panel (A2UI cards + chat). Exists
 * so the Studio shell compiles while the assistant is built in parallel.
 */
import type { CopilotPanelProps } from '../types';

export function CopilotPanel(_props: CopilotPanelProps) {
  return <div>Assistant — coming soon.</div>;
}
